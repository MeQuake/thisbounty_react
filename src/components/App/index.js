import React from 'react';

require('./index.css');

export default class AppComponent extends React.Component {
  render() {
    return (
      <section id="points">
	  /* top banner */
	  
	  /* points-content */
      </section>
    );
  }
}
